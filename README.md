# OOPZ+

OOPZ+ 是一个面向 Windows 的 OOPZ 附属增强工具，核心目标是让多账号使用更顺手：保存已登录过的账号数据，在主程序、托盘菜单和插件浮层里快速切换账号。

> OOPZ+ 不修改 OOPZ 程序文件，不注入 DLL，不逆向登录协议。它只在当前 Windows 用户环境中备份、恢复 OOPZ 的本地账号数据，并提供更方便的入口。

## 主要功能

- 账号快速切换：保存已登录过的账号，一键关闭并重启 OOPZ，切换到目标账号。
- 插件模式：只打开 OOPZ 时，也能自动显示 OOPZ+ 头像浮层。
- 浮层布局：支持横排、竖排切换，通过右上角移动锚调整相对位置并自动保存。
- 登录页浮层：在 OOPZ 登录界面展示本地账号头像，可快速切号或引导登录一次。
- 主界面浮层：登录后在 OOPZ 主界面展示同样的账号头像入口。
- 托盘切号：右键托盘图标快速切换账号。
- 当前账号提示：当前登录账号在主程序、托盘和浮层中不可重复点击。
- OOPZ 路径识别：支持自动搜索 OOPZ 安装目录，也支持手动指定目录。
- 账号导入导出：可把已保存的账号登录数据导出为文件，在其他客户端导入。
- 安全保存：账号密码不写入明文配置文件，优先使用系统安全存储。
- 自动备份：切换前备份当前 OOPZ 数据，异常时可恢复最近一次备份。
- 自动更新：每半小时从 GitHub Release 检查并校验新版，自动安装、重启和修复插件环境。

## 1.0.3 更新

- 修复 OOPZ+ 主窗口偶发被 OOPZ 抢回焦点、无法点击操作的问题。
- 移除主窗口唤醒时瞬间置顶再取消造成的窗口层级反弹。
- 鼠标激活 OOPZ+ 时在松开按键后确认焦点，避免任务栏切换失效。

## 1.0.2 更新

- 自动更新在应用持续运行期间每半小时检查一次，不再只在启动时检查。
- 修复插件浮层每秒重新挂载造成的闪烁和位置重置感。
- 修复高 DPI 下第六个账号头像被裁切，以及竖排窗口仍横向排列的问题。
- 优化浮层边框、四向移动锚和横竖布局的显示效果。

## 1.0.1 更新

- 修复远程头像地址过期后，可切换账号头像丢失的问题。
- 成功加载的头像会保存在本地，后续不再依赖临时 CDN 地址。
- 切号后自动刷新头像，加载失败时显示账号首字占位。
- 阻止主窗口、托盘和插件浮层同时发起切号操作。
- 主程序改为单实例运行，重复启动时显示并置顶已有窗口。
- 插件浮层支持横排、竖排切换和拖动定位，并在重启后恢复上次设置。
- 支持从 GitHub Release 自动检查、校验、下载安装更新，更新后自动重启并修复插件环境。
- 配置、账号快照、导入数据和切号备份改为完整写入后再替换。
- 修复后台刷新重置账号选择、改名产生重复账号和事件重复注册问题。
- 完善重复提交拦截、错误提示、凭据校验和密码自动隐藏。

## 使用说明

### 1. 首次打开

打开 OOPZ+ 后，先在「概览」确认 OOPZ 路径是否已识别。未识别时可以点击自动搜索，或手动选择包含 `oopz.exe` 的目录。

### 2. 保存账号

在 OOPZ 中登录一次目标账号，然后回到 OOPZ+ 的「账号切换」页面刷新账号。保存成功后，该账号会显示为可快速切换。

### 3. 快速切号

在「账号切换」页面点击账号后面的快速切号按钮，或使用托盘菜单、插件浮层头像进行切换。

切换时 OOPZ+ 会：

1. 关闭当前 OOPZ。
2. 备份当前 OOPZ 本地数据。
3. 恢复目标账号的数据。
4. 重新启动 OOPZ。

### 4. 插件模式

在「插件模式」页面开启插件模式后，OOPZ+ 会安装一个轻量守护进程。之后即使没有手动打开 OOPZ+ 主窗口，只打开 OOPZ 也会自动出现账号头像浮层。

插件模式只显示贴附在 OOPZ 上的浮层，不显示 OOPZ+ 主窗口和托盘图标。只有用户手动打开 OOPZ+ 时，主窗口和托盘才会出现。

## 状态标识

- 绿色对钩：该账号可快速切换。
- 黄色感叹号：该账号需要先登录一次。
- 当前登录：当前正在使用的账号，不会重复切换。

## 安全边界

OOPZ+ 的 v1.0.3 设计边界如下：

- 不修改 OOPZ 程序文件。
- 不注入 OOPZ 进程。
- 不调用或逆向 OOPZ 登录接口。
- 不在 JSON 配置中保存明文密码。
- 默认只支持当前 Windows 用户环境。
- 账号快速切换依赖已登录过的本地数据。

## 技术栈

- Tauri 2
- Rust
- React
- TypeScript
- Vite
- Windows Registry / Windows 本地安全存储

## 本地开发

环境要求：

- Windows 10/11
- Node.js
- pnpm
- Rust stable
- Tauri 2 构建环境

安装依赖：

```bash
pnpm install
```

开发运行：

```bash
pnpm tauri dev
```

前端构建：

```bash
pnpm build
```

Rust 检查：

```bash
cd src-tauri
cargo check
```

生成安装包：

```bash
pnpm tauri build
```

安装包会生成在：

```text
src-tauri/target/release/bundle/msi/
```

## 常见问题

### 为什么某个账号显示需要登录一次？

说明 OOPZ+ 还没有保存到这个账号可用于快速切换的数据。请先在 OOPZ 中正常登录一次，再回到 OOPZ+ 刷新。

### 插件模式开启后为什么没有浮层？

先确认 OOPZ 正在运行，并在 OOPZ+ 的「插件模式」页面点击「修复环境」。如果刚安装或刚升级，建议重新打开 OOPZ。

### 切号会删除 OOPZ 原有数据吗？

默认不会。OOPZ+ 会在切换前备份当前数据，删除账号时也只删除 OOPZ+ 自己保存的数据，除非用户明确选择清理。

### 可以跨电脑使用吗？

可以使用账号导出导入功能迁移已保存的账号登录数据。导出的文件包含账号本地登录数据，请妥善保管。

## 版本

当前源码版本：`1.0.3`

最新公开 Release：`1.0.3`

## 许可证

暂未指定开源许可证。未经作者授权，请勿用于商业分发或二次发布。
